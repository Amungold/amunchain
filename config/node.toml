[node]
validator_id = 1
listen_addr = "0.0.0.0:9000"
rpc_addr = "0.0.0.0:9070"

[consensus]
initial_validators = 4
round_timeout_ms = 5000

[chain]
genesis_block = "genesis.bin"

[storage]
db_path = "/tmp/amun-node-data"

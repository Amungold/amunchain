[package]
name = "amun-constitutional-proof"
version = "0.1.0"
edition = "2021"
description = "Constitutional proof framework — obligation registry and verdict engine"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
blake3 = "1"
hex = "0.4"

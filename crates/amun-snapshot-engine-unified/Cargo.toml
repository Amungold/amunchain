[package]
name = "amun-snapshot-engine-unified"
version = "0.1.0"
edition = "2021"
description = "Constitutional snapshot engine for state recovery"
license = "MIT OR Apache-2.0"

[dependencies]
amun-persistence = { path = "../amun-persistence" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

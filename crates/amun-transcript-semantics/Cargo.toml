[package]
name = "amun-transcript-semantics"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
amun-replay-semantics = { path = "../amun-replay-semantics" }
sha2 = { version = "0.11", default-features = false }
hex = "0.4"

[package]
name = "amun-transactions"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "Constitutional transaction types for AmunChain"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
hex = "0.4"
ed25519-dalek = "2"
rand = "0.8"

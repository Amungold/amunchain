[package]
name = "amun-persistence"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Constitutional persistent state recovery for AmunChain"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

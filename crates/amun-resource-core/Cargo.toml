[package]
name = "amun-resource-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Constitutional resource identity, lineage, and transformation matrix"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
hex = "0.4"
thiserror = "2"

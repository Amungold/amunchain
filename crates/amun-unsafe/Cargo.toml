[package]
name = "amun-unsafe"
version = "1.0.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "Sole unsafe boundary for the sovereign kernel"

[lints.rust]
unsafe_code = "allow"

[dependencies]

[package]
name = "amun-entropy-transcript"
version = "1.0.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "AmunChain deterministic entropy transcript for reproducible simulation"

[dependencies]
blake3 = { version = "1.5", default-features = false }

[package]
name = "amun-replay"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "Constitutional replay system: commit log, validation, and certificates"

[dependencies]
amun-constitutional-state = { path = "../amun-constitutional-state" }
blake3 = "1"
serde = { version = "1", features = ["derive"] }
hex = "0.4"

[package]
name = "amun-constitutional"
license = "MIT OR Apache-2.0"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = []

[dependencies]
sha2 = { version = "0.11", default-features = false }
hex = "0.4"

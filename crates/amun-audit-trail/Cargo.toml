[package]
name = "amun-audit-trail"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "Constitutional audit trail linking all trust layers"

[dependencies]
serde = { version = "1", features = ["derive"] }
blake3 = "1"

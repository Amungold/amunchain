[package]
name = "amun-constitutional-state"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "Canonical state runtime with SMT commitment for constitutional state domains"

[dependencies]
amun-constitutional-commitments = { path = "../amun-constitutional-commitments" }
serde = { version = "1", features = ["derive"] }
hex = "0.4"
blake3.workspace = true

[package]
name = "amun-validator-api"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "AmunChain Validator Platform API — frozen constitutional contract, zero dependencies"

[lib]
path = "src/lib.rs"

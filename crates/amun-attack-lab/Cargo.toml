[package]
name = "amun-attack-lab"
version = "1.0.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "AmunChain adversarial simulation laboratory"

[dependencies]
amun-deterministic-allocator = { path = "../amun-deterministic-allocator" }
amun-entropy-transcript = { path = "../amun-entropy-transcript" }

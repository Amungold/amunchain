[package]
name = "amun-validator-bootstrap"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "AmunChain Validator Bootstrap Service — coordinates identity verification and runtime startup"

[dependencies]
amun-validator-api = { path = "../amun-validator-api" }
amun-validator-runtime = { path = "../amun-validator-runtime" }
amun-validator-identity = { path = "../amun-validator-identity" }

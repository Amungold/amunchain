[package]
name = "amun-constitution-builder"
version = "1.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Constitutional artifact compiler and canonical emitter"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
hex = "0.4"

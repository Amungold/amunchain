[package]
name = "amun-chunked-snapshot"
version = "1.0.0"
edition = "2021"
description = "AmunChain chunked snapshot with Merkle proofs"

[dependencies]
amun-snapshot-constitution = { path = "../amun-snapshot-constitution" }
blake3 = { version = "1.5", default-features = false }

[package]
name = "amun-storage-engine"
version = "2.0.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "AmunChain atomic storage engine"

[dependencies]
blake3 = { version = "1.5", default-features = false }

[package]
name = "amun-cross-contract"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Cross-contract resource transfer with cryptographic proofs"

[dependencies]
amun-resource-core = { path = "../amun-resource-core" }
amun-evidence-engine = { path = "../amun-evidence-engine" }
serde = { version = "1", features = ["derive"] }
blake3 = "1"

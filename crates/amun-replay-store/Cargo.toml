[package]
name = "amun-replay-store"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Constitutional replay journal store"

[dependencies]
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"

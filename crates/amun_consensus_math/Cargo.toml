[package]
name = "amun_consensus_math"
version = "1.0.0"
edition = "2021"
authors = ["AmunChain Engineering"]
description = "Constitutional deterministic math kernel for consensus"
license = "Apache-2.0"

[lib]
crate-type = ["lib"]

[dependencies]
sha2 = "0.10"


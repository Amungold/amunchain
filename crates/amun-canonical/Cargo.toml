[package]
name = "amun-canonical"
version = "4.0.0"
edition = "2021"
description = "AmunChain deterministic canonical encoding"

[dependencies]
blake3 = { version = "1.5", default-features = false }
heapless = "0.8"

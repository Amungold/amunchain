[package]
name = "amun-wal"
version = "1.0.0"
edition = "2021"
description = "AmunChain append-only write-ahead log with fsync"

[dependencies]
amun-protocol-event = { path = "../amun-protocol-event" }
blake3 = { version = "1.5", default-features = false }

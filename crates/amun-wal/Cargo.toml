[package]
name = "amun-wal"
version = "1.0.0"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
crc32fast = "1"
sha2 = "0.10"

[package]
name = "amun-chain-position"
version = "1.0.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "AmunChain epoch-bounded chain position"

[dependencies]
blake3 = { version = "1.5", default-features = false }
serde = { version = "1", features = ["derive"] }

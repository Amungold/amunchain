[package]
name = "amun-constitutional-commitment"
version = "0.1.0"
edition = "2021"
description = "CCA v1.0 Constitutional Commitment types, serialization, and root computation"

[dependencies]
blake3 = "1.5"
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"

[features]
default = []

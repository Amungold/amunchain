[package]
name = "amun-kernel-types"
version = "1.0.0"
edition = "2021"
description = "Sovereign kernel primitive types"

[dependencies]
amun-failure = { path = "../amun-failure" }
zeroize = { version = "1.8", default-features = false, features = ["derive"] }

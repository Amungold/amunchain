[package]
name = "amun-constitutional-commitments"
version = "1.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Cryptographic commitment layer using sparse Merkle trees for constitutional state domains"

[dependencies]
blake3 = "1"
serde = { version = "1", features = ["derive"] }

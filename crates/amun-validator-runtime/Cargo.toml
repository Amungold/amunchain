[package]
name = "amun-validator-runtime"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "AmunChain Validator Runtime — production-grade atomic state machine"

[dependencies]
amun-validator-api = { path = "../amun-validator-api" }

[package]
name = "amun-pacemaker"
version = "1.0.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "AmunChain consensus pacemaker"

[dependencies]
amun-chain-position = { path = "../amun-chain-position" }
amun-deterministic-timer = { path = "../amun-deterministic-timer" }

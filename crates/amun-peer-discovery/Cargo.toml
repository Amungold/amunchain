[package]
name = "amun-peer-discovery"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Peer discovery and network bootstrap"

[dependencies]
serde = { version = "1", features = ["derive"] }
postcard = { version = "1", features = ["use-std"] }
tempfile = "3"

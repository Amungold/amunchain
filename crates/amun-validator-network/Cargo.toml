[package]
name = "amun-validator-network"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "AmunChain Validator Networking Service — Mainnet-grade P2P stack"

[dependencies]
amun-validator-api = { path = "../amun-validator-api" }
amun-validator-identity = { path = "../amun-validator-identity" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"

[package]
name = "amun-light-client"
version = "1.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Constitutional light client — verifies certificates, chains, and history roots"

[dependencies]
amun-resource-core = { path = "../amun-resource-core" }
amun-transition-proof = { path = "../amun-transition-proof" }
amun-constitutional-runtime = { path = "../amun-constitutional-runtime" }
serde = { version = "1", features = ["derive"] }

[package]
name = "amun-authority-registry"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
hex = "0.4"
postcard = { version = "1", features = ["use-std"] }
amun-networking = { path = "../amun-networking" }

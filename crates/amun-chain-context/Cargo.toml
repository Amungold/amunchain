[package]
name = "amun-chain-context"
version = "1.0.0"
edition = "2021"
description = "AmunChain sovereign chain identity context"

[dependencies]
amun-invariants = { path = "../amun-invariants" }
blake3 = { version = "1.5", default-features = false }
serde = { version = "1", features = ["derive"] }

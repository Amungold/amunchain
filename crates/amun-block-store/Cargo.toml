[package]
name = "amun-block-store"
version = "0.1.0"
edition = "2021"
description = "Constitutional block store for chain history"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

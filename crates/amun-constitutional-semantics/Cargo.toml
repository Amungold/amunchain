[package]
name = "amun-constitutional-semantics"
version = "0.1.0"
edition = "2021"

[dependencies]
amun-kernel = { path = "../amun-kernel" }
amun-replay-semantics = { path = "../amun-replay-semantics" }
amun-transcript-semantics = { path = "../amun-transcript-semantics" }
sha2 = { version = "0.10", default-features = false }
serde = { version = "1", features = ["derive"], default-features = false }

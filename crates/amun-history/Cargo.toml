[package]
name = "amun-history"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "AmunChain History Root computation (ADR-024)"

[dependencies]
blake3 = "1.5"

[dev-dependencies]

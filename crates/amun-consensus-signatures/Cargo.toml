[package]
name = "amun-consensus-signatures"
version = "1.0.0"
edition = "2021"
description = "AmunChain consensus signature domain separation"

[dependencies]
blake3 = { version = "1.5", default-features = false }

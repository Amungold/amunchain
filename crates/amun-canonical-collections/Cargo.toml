[package]
name = "amun-canonical-collections"
version = "0.3.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"], default-features = false }
amun-kernel = { path = "../amun-kernel" }
sha2 = { version = "0.10", default-features = false }

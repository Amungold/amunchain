[package]
name = "amun-tls"
version = "4.0.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "AmunChain TLS 1.3 with mTLS and certificate pinning"

[dependencies]
rustls = { version = "0.23", features = ["tls12"] }
rustls-pemfile = "2"
sha2 = "0.11"
hex = "0.4"
webpki-roots = "0.26"

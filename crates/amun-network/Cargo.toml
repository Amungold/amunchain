[package]
name = "amun-network"
version = "1.0.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "Peer-to-peer network layer for AmunChain"

[dependencies]
amun-kernel-types = { path = "../amun-kernel-types" }
heapless = "0.8"
hashbrown = "0.14"

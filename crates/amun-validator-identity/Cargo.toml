[package]
name = "amun-validator-identity"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "AmunChain Validator Identity Service — single source of truth for identity, keys, and authorities"

[dependencies]
amun-validator-api = { path = "../amun-validator-api" }
ed25519-dalek = { version = "2", features = ["rand_core"] }

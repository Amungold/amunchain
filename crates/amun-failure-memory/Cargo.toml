[package]
name = "amun-failure-memory"
version = "1.0.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "AmunChain immunological failure memory"

[dependencies]
amun-invariants = { path = "../amun-invariants" }
blake3 = { version = "1.5", default-features = false }

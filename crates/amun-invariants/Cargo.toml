[package]
name = "amun-invariants"
version = "1.0.0"
license = "MIT OR Apache-2.0"
edition = "2021"
description = "AmunChain irreducible survival invariants"

[dependencies]
blake3 = { version = "1.5", default-features = false }
heapless = "0.8"

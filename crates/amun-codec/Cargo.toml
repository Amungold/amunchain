[package]
name = "amun-codec"
version = "1.0.0"
edition = "2021"
description = "Canonical deterministic serialization codec"

[dependencies]
amun-failure = { path = "../amun-failure" }
amun-kernel-types = { path = "../amun-kernel-types" }
blake3 = { version = "1.5", default-features = false }
heapless = "0.8"

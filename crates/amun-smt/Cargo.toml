[package]
name = "amun-smt"
version = "0.4.0"
edition = "2021"
description = "Canonical authenticated Sparse Merkle Patricia Trie — consensus-hardened"

[dependencies]
blake3 = "1.5"
thiserror = "1.0"
once_cell = "1.19"

[dev-dependencies]
rand = "0.8"
proptest = "1.4"

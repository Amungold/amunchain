[package]
name = "amun-determinism-tests"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
amun-deterministic-allocator = { path = "../../crates/amun-deterministic-allocator" }
